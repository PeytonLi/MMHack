# @mmhack/web Codex Notes

- This UI must work well on a phone over HTTPS.
- The first workflow step is SKU selection, not auto-identification.
- Camera behavior is live preview with explicit capture, not continuous scoring.
- The app should make low-confidence and failed-write states obvious to the operator.
- Keep the UX narrow: select SKU, capture, review result, then inspect audit history.
